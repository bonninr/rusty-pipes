[target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture arm64",
    "apt-get update && apt-get install -y libasound2-dev libjack-jackd2-dev libpulse-dev libx11-dev libxkbcommon-dev libwayland-dev libegl-dev libgl-dev cmake"
]
